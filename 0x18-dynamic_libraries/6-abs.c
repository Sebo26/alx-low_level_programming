#include "main.h"
#include <stdlib.h>

/**
 * _abs - Computes absolute value of an integer
 *
 * @m: The number which will have its absolute value computed.
 * Return: n
 */
int _abs(int m)
{
int n;
n = abs(m);

return (n);
}
