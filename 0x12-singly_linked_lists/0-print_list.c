#include "lists.h"

/**
 * print_list - The function prints all the elements of a list_t list.
 * @h: pointer to elements of list_t
 * Return: the number of nodes
 */
size_t print_list(const list_t *h)
{
int i;
if (str == NULL)
{
	printf("[0] (nil)");
}

for (i = 0; i 
