#include "main.h"

/**
 * add - it add two integers and returns a result.
 *
 * @m: first number of the sum.
 * @n: second number of the sum.
 * Return: s (result of adding the two integer)
 */
int add(int m, int n)
{
int s;
s = m + n;

return (s);
}
