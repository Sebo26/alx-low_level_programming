#include "main.h"
#include <string.h>

/**
 * rev_string - The function reverses a string.
 * @s: The string to be reversed
 */
void rev_string(char *s)
{
int i;
int l = strlen(s);

for (i = l - 1; i >= 0; i--)
}
