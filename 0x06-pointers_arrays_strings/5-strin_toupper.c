#include "main.h"
#include <string.h>

/**
 * *string_toupper - The function changes all lowercase letters of a string to uppercase.
 *
 * @*: The string that will have lowercase letters changed to uppercase.
 * Return: * (The string that has been changed to uppercase.
 */
char *string_toupper(char *)
{       
int length;
length = strlen(s);
int i;
for (i = 0; i < length; i++)
{
	*[i] = toupper(s]i]);
}
}
