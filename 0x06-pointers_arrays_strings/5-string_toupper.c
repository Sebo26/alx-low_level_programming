#include "main.h"
#include <string.h>

/**
 * *string_toupper - The function changes all lowercase letters of a
 * string to uppercase.
 * @*: The string that will change lowercase letters to uppercase.
 * Return: * (String that will have lowercase changed into uppercase.
 */
char *string_toupper(char *)
{
int length;
int length = strlen(*);
int i;
for (i = 0; i < length; i++)
{
	s[i] = toupper(*[i]);
}
return (*);
}
