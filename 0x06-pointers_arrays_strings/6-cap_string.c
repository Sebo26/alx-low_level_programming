#include "main.h"
#include <string.h>

/**
 * *cap_string - The function capitalizes all words of a string.
 * @s: The string that will have all words capitalized.
 * Return: s ( the string capitalised)
 */
char *cap_string(char *)

