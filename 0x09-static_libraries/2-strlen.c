#include "main.h"
#include <string.h>

/**
 * _strlen- The function returns the length of a string.
 *
 * @s: The string that will have its length determined.
 * Return: l (length)
 */
int _strlen(char *s)
{
int l;
l = strlen(s);

return (l);
}
